# Extension (migre)

Le plugin WordPress "WP PK Premium" a ete separe dans un repo dedie : `WP_pkpremium`.

Dans ce repo `WP_Snippets`, le dossier `extension/` est obsolette et sera supprime apres migration.
